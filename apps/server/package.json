{
  "name": "@spoonos/server",
  "private": true,
  "scripts": {
    "dev": "uvicorn spoonos_server.api.app:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn spoonos_server.api.app:app --host 0.0.0.0 --port 8000",
    "test": "pytest"
  }
}
